// contador negativo

let contador = 100;
contador--;

console.log("O resultado do decremento é: " + contador);

// interpolação

const nome = "Jairo";
console.log(`Olá, ${nome}! Bem vindo ao nosso sistema.`);

// interpolação com operações matemáticas

const a = 5;
const b = 3;

console.log(`A soma de ${a} + ${b} é ${a+b}.`);

// interpolação dentro de um objeto

const usuario = {
    nome: "Cleisson Oswaldo Robert da Cruz",
    idade: 30,
    cidade: "Rio de Janeiro"
};

console.log(`O cliente ${usuario.nome} possui ${usuario.idade} anos e mora no ${usuario.cidade}.`);

